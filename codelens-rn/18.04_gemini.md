# CodeLens Phase 6 QA - Status Handoff (18.04.2026)

## Where We Are
We are in the middle of executing the **Phase 6 QA Runbook (Backup & Restore)**. 

### Accomplished Today:
1. **Architectural Review & Hardening:**
   - Validated the Hot/Cold vector tier (FTS5 virtual table for cold storage).
   - Validated Drizzle Transaction context fixes.
   - Validated `expo-crypto` UUIDs to prevent ID collisions.
2. **Bug Fixes:**
   - Fixed the `app.json` Android C++ build cache issue by manually wiping `android/app/.cxx`, `android/app/build`, and `android/.gradle` after enabling `fts5: true`.
   - Fixed the Home screen header layout (`app/index.tsx`) where the Settings button was being pushed off-screen due to flexbox constraints. Wrapped buttons in a horizontal `ScrollView`.
   - Fixed Android keyboard obscuring chat inputs by setting `behavior="padding"` on `KeyboardAvoidingView` across all chat screens.
   - Fixed API key trailing whitespace bugs by adding `.trim()` to `src/adapters/openrouter-client.ts` and `src/adapters/siliconflow-client.ts`.
3. **Environment Setup:**
   - Successfully connected the physical Android device via ADB.
   - Fixed Metro Bundler connection drops (`adb reverse tcp:8081 tcp:8081`).
   - Configured API keys and set up the optimal free model stack via ADB injection.

### Current Model Stack:
- **General Chat:** `meta-llama/llama-3.3-70b-instruct:free` (OpenRouter)
- **Section Chat:** `deepseek-ai/DeepSeek-R1-Distill-Qwen-7B` (SiliconFlow)
- **Learning (Extraction):** `Qwen/Qwen2.5-7B-Instruct` (SiliconFlow) - *Must output strict JSON*
- **Embedding (Vector):** `BAAI/bge-small-en-v1.5` (SiliconFlow) - *Required for 384-dim sqlite-vec constraint*

## Next Steps (Tomorrow)

### 1. Finish the "Golden Dataset"
Before testing the backup export, the app database must be seeded with test data:
- Create 2 Projects with 4+ Files.
- Make highlights/marks on the code.
- Send 4+ messages in a General Chat.
- Send 4+ messages in a Section Chat.
- **CRITICAL:** Long-press at least 3 AI chat bubbles and tap "Save as Learning". Verify the Concepts and Sessions appear in the Learning Hub.
- Run the RAG Smoke Test on the Dev screen.

### 2. Execute Backup & Restore Audit
- Export the `.codelens` archive from the Settings screen.
- Run the PowerShell audit script (from the QA Runbook) to verify the NDJSON files, metadata, and Base64 vector encoding.
- Wipe the app (Fresh Install or "Clear All Data").
- Restore from the `.codelens` backup and verify UI parity, RAG functionality, and that API keys were *not* restored (security feature).

### 3. Future Enhancements (Post-V1.0)
- **Model Fallbacks:** Update `src/ai/queue.ts` to accept an array of fallback models to automatically recover from `429 Rate Limited` errors (especially on free tiers).
- **Embedding Fallback:** If SiliconFlow goes down, fallback to OpenRouter using OpenAI's `text-embedding-3-small` by explicitly passing the `dimensions: 384` parameter to satisfy the SQLite constraint.
